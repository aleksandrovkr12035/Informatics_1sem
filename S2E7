raw = input().split()

max_count = 0
most = raw[0]


for i in range(len(raw)):
    count = 0

    for j in range(len(raw)):
        if raw[i] == raw[j]:
            count += 1
    
    if count > max_count:
        max_count = count
        most = raw[i]

print(max_count)